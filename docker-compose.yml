services:
  app:
    build: ./app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - OLLAMA_HOST=ollama:11434
    depends_on:
      - ollama
    runtime: nvidia

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama
    runtime: nvidia

volumes:
  ollama:
